pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "BTExample"

include ':app',
        ':BlueSTSDK'

project(':BlueSTSDK').projectDir = new File('BlueSTSDK_Android/BlueSTSDK')
